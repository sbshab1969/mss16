#Generated by ResourceBundle Editor (http://eclipse-rbe.sourceforge.net)

add_line = add_line

add_line.value = add_line.value

add_str = add_str

add_str.value = add_str.value

add_zone_code = add_zone_code

add_zone_code.value = add_zone_code.value

algoritm = algoritm

amts_trunks = amts_trunks

ats = ats

ats.name = ats.name
ats.type = ats.type

backup = backup

backup.dir = backup.dir

bad = bad

bad.ext          = bad.ext

bad_record_types = bad_record_types

change = change

change.lac = change.lac

code_region = code_region

code_region.value = code_region.value

del_zone_code = del_zone_code

del_zone_code.value = del_zone_code.value

division = division

division.value = division.value

duration = duration

duration.limit = duration.limit

dvo = dvo

dvo.ext = dvo.ext

eml.mailer.eml.PROTOCOL = eml.mailer.eml.PROTOCOL

field = field

field.add_digit = field.add_digit
field.id        = field.id
field.imsi      = field.imsi
field.key       = field.key
field.mask      = field.mask
field.max       = field.max
field.min       = field.min
field.name      = field.name
field.ndel      = field.ndel
field.num       = field.num
field.order     = field.order
field.size      = field.size
field.value     = field.value

filter = filter

filter.ext          = filter.ext
filter.filter_field = filter.filter_field

filter_prior = filter_prior

filter_prior.value = filter_prior.value

filters = filters

fixed_fields = fixed_fields

fixed_fields.value = fixed_fields.value

fld_separator = fld_separator

fld_separator.char = fld_separator.char

fmt = fmt

fr_atm = fr_atm

fr_atm.ext = fr_atm.ext

gprs = gprs

gprs.ext = gprs.ext

home = home

in_sms = in_sms

in_sms.ext = in_sms.ext

input = input

input.dir = input.dir

int_inn = int_inn

int_inn.ext = int_inn.ext

int_rouming = int_rouming

int_rouming.ext = int_rouming.ext

internet = internet

internet.ext  = internet.ext

internet_nmbs = internet_nmbs

interval = interval

local_nmbs = local_nmbs

log = log

log.filename = log.filename

mask = mask

mask.value = mask.value

min_valid = min_valid

min_valid.date = min_valid.date

operator = operator

out_inn = out_inn

out_inn.ext = out_inn.ext

out_sms = out_sms

out_sms.ext = out_sms.ext

output = output

output.dir    = output.dir
output.file   = output.file
output.mode   = output.mode

output_format = output_format

partial = partial

partial.filename = partial.filename

path = path

path.name = path.name

pilot_nmbs = pilot_nmbs

ptfop = ptfop

ptfop.value = ptfop.value

pvs = pvs

pvs.value = pvs.value

real_msc = real_msc

recipient = recipient

recipient.value = recipient.value

rewrite = rewrite

rewrite.output_field = rewrite.output_field
rewrite.step         = rewrite.step
rewrite.value        = rewrite.value

rouming = rouming

rouming.ext = rouming.ext

rule = rule

rule.action       = rule.action
rule.condition    = rule.condition
rule.input_fields = rule.input_fields
rule.name         = rule.name
rule.path         = rule.path
rule.step         = rule.step
rule.type         = rule.type

sender = sender

sender.value = sender.value

sid_pref = sid_pref

sid_pref.field = sid_pref.field

sid_src = sid_src

sid_src.filename = sid_src.filename

split_internet = split_internet

split_internet.value = split_internet.value

subdir = subdir

subdir.subdir_fields = subdir.subdir_fields

sverka.ats      = sverka.ats
sverka.ats.type = sverka.ats.type

tfop_mask = tfop_mask

time = time

time.offset = time.offset

tmp_format = tmp_format

tmp_format.name = tmp_format.name
tmp_format.size = tmp_format.size

total = total

total.ext = total.ext

trace_sop = trace_sop

trace_sop.field = trace_sop.field

transit = transit

transit.ext    = transit.ext

transit_trunks = transit_trunks

unite_str = unite_str

unite_str.value = unite_str.value

unknown = unknown

unknown.ext = unknown.ext

valid_prefix = valid_prefix

vtn = vtn

vtnpilot = vtnpilot
