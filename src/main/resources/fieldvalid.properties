#Generated by ResourceBundle Editor (http://eclipse-rbe.sourceforge.net)

add_line = value

add_str = value

add_zone_code = value

algoritm = rule

amts_trunks = field

ats = type,name,input,output,backup,log,time,tmp_format,fld_separator,duration,min_valid,bad_record_types,fmt

backup = dir

bad_record_types = field

change = field

change_lac = field

code_region = value

del_zone_code = value

division = value

duration = limit

dvo = ext

eml.mailer = eml.name,eml.PROTOCOL,eml.HOST,eml.USER,eml.PASSWORD,eml.MAILHOST,eml.MAILER,eml.CHARSET,eml.file_dir,eml.arch_dir,\neml.file_pattern,eml.from,eml.logsql,eml.loglevel

field = name,order,size,id,num,imsi,min,max,key,value,mask,ndel,add_digit

filter = ext,filter_field,mask,interval

filter_prior = value

filters = filter

fixed_fields = value

fld_separator = char

fmt = output,fld_separator,del_zone_code,fixed_fields,log,partial,total,unknown,rouming,int_rouming,internet,out_inn,int_inn,in_sms,out_sms,dvo,transit,gprs,fr_atm,split_internet,time,sender,add_zone_code,division,filter_prior,recipient,add_str,unite_str,output_format,real_msc,home,operator,valid_prefix,local_nmbs,change,change_lac,internet_nmbs,amts_trunks,transit_trunks,filters,subdir,pvs,ptfop,code_region,vtn,vtnpilot,pilot_nmbs,tfop_mask,add_line,sid_src,sid_pref,trace_sop,algoritm

fr_atm = ext

gprs = ext

home = field

in_sms = ext

input = dir

int_inn = ext

int_rouming = ext

internet = ext

internet_nmbs = field

interval = field

local_nmbs = field

log = filename

mask = value

min_valid = date

operator = field

out_inn = ext

out_sms = ext

output = file,dir,mode

output_format = field

partial = filename

pilot_nmbs = field

ptfop = value

pvs = value

real_msc = field

recipient = value

rewrite = output_field,value,step

rouming = ext

rule = rewrite,type,action,step,name,path,input_fields,condition

sender = value

sid_pref = field

sid_src = filename

split_internet = value

subdir = subdir_fields,field

sverka.ats = type

tfop_mask = field

time = offset

tmp_format = field,name,size

total = ext

trace_sop = field

transit = ext

transit_trunks = field

unite_str = value

unknown = ext

valid_prefix = field

vtn = field

vtnpilot = field
